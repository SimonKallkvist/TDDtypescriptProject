// -_-

import CheckoutForm from "../../components/checkoutform/CheckoutForm";

const CheckoutPage = () => {
  return (
    <section>
      <CheckoutForm />
    </section>
  );
};

export default CheckoutPage;
